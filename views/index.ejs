<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chess Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen flex items-center justify-center bg-zinc-900">
      <div class="chessboard w-96 h-96 bg-red-400"></div>
    </div>

    <script
      src="https://cdn.socket.io/4.8.1/socket.io.min.js"
      integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"
      integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    ></script>
    <script src="/javascripts/chessgame.js"></script>
    <style>
      /* Basic layout of the chessboard */
      .chessboard {
        display: grid;
        grid-template-columns: repeat(8, minmax(0, 1fr));
        grid-template-rows: repeat(8, minmax(0, 1fr));
        width: 400px;
        height: 400px;
        transform: rotate(0deg);
        border: 2px solid #333; /* Adding a border around the chessboard */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3); /* Adding shadow for a 3D effect */
      }

      /* Adding a gradient background for the page */
      body {
        background: linear-gradient(135deg, #2d3748, #1a202c);
        font-family: "Arial", sans-serif;
      }

      /* Styling for the game container */
      .w-full {
        background-color: #2d3748;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      /* Styling for chessboard squares */
      .square {
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background-color 0.3s ease; /* Smooth transition for square colors */
        cursor: pointer; /* Cursor pointer on hover */
      }

      /* Light square styling */
      .square.light {
        background-color: #f0d9b5;
      }

      /* Dark square styling */
      .square.dark {
        background-color: #b58863;
      }

      /* Hover effect for chessboard squares */
      .square:hover {
        background-color: #d1b38b; /* Slightly darker shade for hover effect */
      }

      /* Piece styling for both white and black pieces */
      .piece.white {
        color: white;
        filter: drop-shadow(
          0 0 3px rgba(0, 0, 0, 0.8)
        ); /* Stronger shadow for white pieces */
        transition: transform 0.1s ease-in-out; /* Smooth piece movement */
      }

      .piece.black {
        color: black;
        filter: drop-shadow(
          0 0 3px rgba(255, 255, 255, 0.8)
        ); /* White shadow for black pieces */
        transition: transform 0.1s ease-in-out; /* Smooth piece movement */
      }

      /* Flipped board for black player */
      .flipped {
        transform: rotate(180deg);
      }

      /* Styling for draggable pieces */
      .piece {
        font-size: 36px;
        cursor: grab;
        user-select: none; /* Prevent text selection while dragging */
        transition: transform 0.1s ease-in-out;
      }

      .piece:active {
        cursor: grabbing; /* Change cursor when dragging */
        transform: scale(1.2); /* Slightly enlarge piece while dragging */
      }

      /* Dragging piece with opacity */
      .dragging {
        opacity: 0.6; /* Slight transparency while dragging */
      }

      /* Styling for game over notification */
      .game-over {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        padding: 20px;
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        font-size: 24px;
        font-weight: bold;
        border-radius: 10px;
        text-align: center;
        display: none; /* Hidden by default */
      }

      /* Show the game over notification when the game ends */
      .game-over.show {
        display: block;
      }

      /* Styling for players' roles (optional: display player role in top corner) */
      .player-role {
        position: absolute;
        top: 20px;
        left: 20px;
        color: white;
        font-size: 18px;
        font-weight: bold;
        padding: 10px;
        background-color: rgba(0, 0, 0, 0.5);
        border-radius: 5px;
      }

      /* Adding a loading spinner while waiting for the game to start */
      .loading-spinner {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        font-size: 24px;
        color: white;
      }

      .chessboard.flipped .piece {
        transition: transform 0.5s ease-in-out; /* Smooth spin effect */
        transform: rotate(180deg);
      }

      .chessboard:not(.flipped) .piece {
        transition: transform 0.5s ease-in-out;
        transform: rotate(0deg);
      }

      /* Spinner animation */
      .loading-spinner::before {
        content: "⏳";
        animation: spin 1s infinite linear;
      }

      /* Spinner animation keyframes */
      @keyframes spin {
        0% {
          transform: rotate(0deg);
        }
        100% {
          transform: rotate(360deg);
        }
      }
    </style>
  </body>
</html>
